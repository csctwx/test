apply plugin: "java"
defaultTasks 'first'
task first << {	task->
	println "Runing ${task.name}"
}

task second << { task->
	println "Runing ${task.name}"
}

second.dependsOn = ['first']

task third(dependsOn:'second') << { task->
	println "Runing ${task.name}"
}